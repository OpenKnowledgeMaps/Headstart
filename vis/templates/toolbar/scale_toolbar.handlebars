<div class="scale-toolbar btn-group dropup">
    <div class="dropdown">
        <button class="btn btn-primary dropdown-toggle" id="scale-menu" type="button" data-toggle="dropdown">
            {{scale_by_label}}
            <span id="curr-scale-type"></span>
            <span class="caret"></span>
        </button>
        <ul class="dropdown-menu" role="menu" aria-labelledby="scale-menu">
        </ul>
    </div>

<div class="context-scale-toolbar">
        <span id="curr-scale-explanation"></span>
<a id="infolink" class="scale-infolink" data-toggle="modal" data-type="text" href="#info_modal"></a>
        <div class="explanation-ids">
            Legende:
            <span class="explanation-id-fields"></span>
        </div>
    </div>
        {{#if credit}}
        <div id="credit">created by: <a href="https://openknowledgemaps.org/" target="_blank"><img class="logoimg" style="border:0px;" src="./img/okmaps-logo.png"></a></div>
        {{/if}}
</div>