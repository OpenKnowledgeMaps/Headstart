export interface Misc {
  isEmbedded: boolean;
  isLoading: boolean;
  showCreatedByViper: boolean;
  showLoading: boolean;
  renderList: boolean;
  renderMap: boolean;
  visTag: "visualization";
  timestamp?: string;
}
